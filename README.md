# METEOR's SIMPLE-TODOS

A simple web app created using Meteor in order to understand how it works (it seems to work fine for little/toy applications).

## Run

Just run `meteor` in the working directory.